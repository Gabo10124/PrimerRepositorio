//Escribimos la frase que se va a modificar
let frase = "     JavaScript es genial    ";

//Elminamos los espacios al inicio y al final, y convertimos a mayusuculas
frase = frase.trim();
frase = frase.toUpperCase();

//Imprimimos el texto modificado
console.log(frase);